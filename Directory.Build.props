<Project>
  <PropertyGroup>
    <!-- Exclude nested output and temporary artifact folders from default item globbing. -->
    <DefaultItemExcludes>$(DefaultItemExcludes);**/bin/**;**/obj/**;**/gitignored/**</DefaultItemExcludes>
  </PropertyGroup>
</Project>
